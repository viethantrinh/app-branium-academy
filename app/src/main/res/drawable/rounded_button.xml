<?xml version="1.0" encoding="utf-8"?>
<!-- res/drawable/rounded_button.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/primary"/> <!-- Màu nền của Button -->
    <corners android:radius="16px"/> <!-- Độ cong của góc -->
    <stroke
        android:width="2dp"
        android:color="@color/primary"/> <!-- Đường viền xung quanh button -->
</shape>